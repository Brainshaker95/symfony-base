{% for type, messages in app.getFlashes %}
  <section class="flash-messages">
    <div class="container">
      {% for message in messages %}
        <div class="alert alert--{{ type }}">
          {{ message|trans }}
        </div>
      {% endfor %}
    </div>
  </section>
{% endfor %}
